# Weather Shortcode

Simple WordPress plugin that registers the `[weather]` shortcode and keeps `[wether]` as a compatibility alias.

## Usage

Activate the plugin and add a shortcode like:

```text
[weather city="Tehran" condition="Sunny" temperature="24" unit="C"]
```

This renders:

```text
Tehran — Sunny — 24°C
```
